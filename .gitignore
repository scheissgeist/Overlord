# Build output
obj/
bin/
Assemblies/*.dll
Assemblies/*.pdb

# IDE
.vscode/
.vs/
*.user
*.suo

# Node
relay-server/node_modules/
relay-server/logs/
relay-server/.env
relay-server/.env.local

# OS
Thumbs.db
.DS_Store

# Local testing artifacts
docs/viewer_ui_smoke*.png

# --- Keep off public GitHub ---
# Personal agent rules (health context, private project registry)
AGENTS.md

# Private ops / session notes (live URLs, emails, instance IDs)
docs/SESSION_LOG_*.md
docs/HANDOFF_*.md
docs/TASK_*.md
docs/IMPLEMENTATION_STATUS.md
docs/PUBLIC_CHECKLIST.md
# Full private launch handoff stays local; short public pointer is docs/LAUNCH_STATUS.md

# Internal planning / reverse-engineering notes (not end-user docs)
docs/PLAN.md
docs/EXECUTION_PLAN.md
docs/FTB_PATTERNS.md
docs/FUTURE_PROOFING_PLAN.md
docs/LOW_LATENCY_REDESIGN.md
docs/PUPPETEER_*.md
docs/RIMWORLD_16_API.md
docs/VIEWER_DATA_RENDERER_*.md

# Personal Fly app name / region — use fly.toml.example
relay-server/fly.toml

# Release staging
dist/
*.zip
data/workshop_stats.csv
# live multi-agent coordination — local ops, never public
docs/AI_CHANNEL.md

# stray generated image at repo root — never commit to public tree
Generated_image.png

# Untracked host-UI screenshots from 2026-07-09, kept on disk but out of the tree.
# Identical shots saved 6 min apart; nothing references either; both predate the
# Inspector heal/cube buttons (238189c) so neither is a usable gallery asset. A
# fresh Workshop shot belongs in docs/images/ off the CURRENT UI. Ignored so they
# stop reading as dirty every session (three sessions re-surveyed them since 07-09).
rimmies.png
docs/images/overlord-workshop-gallery-ui.png
