<?xml version="1.0" encoding="utf-8"?>
<Defs>
  <MainButtonDef>
    <defName>Overlord_MainButton</defName>
    <label>Overlord</label>
    <order>200</order>
    <buttonVisible>true</buttonVisible>
    <!-- No iconPath: the default MainButtonWorker_Toggle renders the <label>
         text ("Overlord"). The star seal lives in the tab's own header + the
         Workshop art instead of on the bottom button (Sean's call 2026-07-13). -->
    <tabWindowClass>Overlord.OverlordMainTabWindow</tabWindowClass>
  </MainButtonDef>
</Defs>
